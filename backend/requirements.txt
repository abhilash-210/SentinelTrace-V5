# ==================================================
# SentinelTrace V5 — Backend Python Dependencies
# ==================================================
# IMPORTANT: Use Python 3.11 or 3.12.
# Python 3.13+ does not yet have pre-built wheels for
# psycopg2-binary and pyarrow. On Python 3.13+, use Docker.
#
# Setup (Python 3.11 / 3.12):
#   cd backend
#   python -m venv .venv
#   .venv\Scripts\activate        (Windows)
#   pip install -r requirements.txt
#
# Docker (any Python / any OS):
#   docker compose up --build
# ==================================================

# ── Web Framework ──────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1

# ── Database (ORM + Migrations) ────────────────────
sqlalchemy==2.0.30
alembic==1.13.1

# ── PostgreSQL driver (Docker / Production) ────────
# Uncomment if using PostgreSQL directly (requires C build tools on Python 3.13+)
# psycopg2-binary==2.9.9

# ── Configuration ──────────────────────────────────
pydantic-settings==2.3.0
python-dotenv==1.0.1

# ── Security ───────────────────────────────────────
bcrypt==4.1.3
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# ── HTTP Utilities ─────────────────────────────────
httpx==0.27.0
python-multipart==0.0.9

# ── Data Lake Export ───────────────────────────────
# Pre-built wheels available for Python 3.11 and 3.12 on PyPI.
# On Python 3.13+, install may require a C compiler.
# These are pre-installed in the provided Docker image.
pandas==3.0.6
pyarrow==25.0.1
